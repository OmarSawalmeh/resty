

import ReactDOM from "react-dom";
//import "./index.css"; // sometimes you gonna see this like it will import the generic css and apply them in all the components that is inside index.js
import App from "./app";
const rootEl = document.getElementById("root");
ReactDOM.render(<App />, rootEl);
